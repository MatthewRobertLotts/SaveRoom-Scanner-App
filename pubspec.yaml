name: saveroom_scanner_app
description: SaveRoom scanner and collector app frontend.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  extended_image: ^10.1.0
  flex_color_scheme: ^8.4.0
  flutter:
    sdk: flutter
  http: ^1.6.0
  lucide_icons_flutter: ^3.1.15
  skeletonizer: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/fixtures/v12_2_pos/
